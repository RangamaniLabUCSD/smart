name: test
channels: 
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pint
  - tabulate
  - sympy
  - fenics
  - pandas
  - matplotlib
  - termcolor
  - numpy
  - scipy

  # Pip-only installs
  - pip:
    - pprint
  #  - codecov

