name: test
channels: 
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pint
  - tabulate
  - sympy
  - dolfin
  - pandas
  - matplotlib
  - termcolor
  - numpy
  - pprint
  - scipy

    # Pip-only installs
  #- pip:
  #  - codecov

